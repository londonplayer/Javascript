
let x = 3.14;
let y = 5;
let z = 9;
let maximum;
let minimum;

//x = Math.round(x);            //rounds the number
//x = Math.floor(x);            //rounds the number down
//x = Math.ceil(x);             //rounds the number up
//x = Math.pow(x, 2);           //calculates the power
//x = Math.sqrt(x);             //calculates the square root
//x = Math.abs(x);              //calculates the absolute number (distance away from 0)
//maximum = Math.max(x, y, z);  //gives the higher value 
//minimum = Math.min(x, y, z);  //gives the lower value
//x = Math.PI;                  //gives PI

console.log(x);